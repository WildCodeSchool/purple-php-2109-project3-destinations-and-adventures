{{ form_start(form) }}
    {{ form_widget(form.name) }}
    {{ form_widget(form.commission, {'attr': {'style': 'display:none'}}) }}
    <button class="btn">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
